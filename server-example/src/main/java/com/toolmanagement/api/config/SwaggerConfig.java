package com.toolmanagement.api.config;

import org.springframework.context.annotation.Configuration;

/**
 * Swagger配置类
 * 已禁用，因为与Spring Boot 2.7.0不兼容
 */
@Configuration
public class SwaggerConfig {
    // 暂时移除Swagger配置
}